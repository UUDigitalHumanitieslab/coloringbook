# Uncomment and edit the following lines if coloringbook isn't already
# on the PYTHONPATH.

# import sys
# 
# sys.path.insert(0, '/absolute/path/to/coloringbook')

import coloringbook

application = coloringbook.create_app()
